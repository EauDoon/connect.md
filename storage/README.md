# Runtime Markdown storage

The API mounts this location for immutable profile and resume versions. Runtime documents are excluded from version control and must be backed up together with PostgreSQL.

