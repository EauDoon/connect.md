# Coverage gaps and manual gates

The platform contract closes inventory drift; it does not prove every integration property. Keep the distinction below explicit when reviewing a feature or making a release claim.

## Enforced by the repository checker

- required feature domains and structurally complete manifests, with every registered feature mapped to the release matrix owner column;
- exact ownership of every FastAPI route, Next.js App Router page, and persistent SQLAlchemy model;
- repository-relative implementation, test, worker, operations, and gate paths;
- existence of declared API and UI route anchors;
- feature-stage, discovery-state, data, lifecycle, and evidence-gate consistency;
- hidden OpenAPI treatment and public-discovery exclusion for gated account lifecycle routes;
- protocol declarations backed by implemented routes and test anchors;
- AST-derived `/llms.txt` workflow anchors for authenticated raw-Markdown create/read/conditional-update and public search;
- MCP strict create/update/proposal schemas, shared durable write-helper reuse, applicable credential instructions, and proposal-only non-mutation tests;
- proposal-decision visible-ASCII idempotency, owner-lock replay ordering, proposal/action/document/version/SHA-256 receipt binding, and tamper-fail-closed reconstruction; Clerk-owner API-key create/list/revoke with normalized scope binding, one-time-secret/metadata-only recovery, secret-free receipt/event anchors, missing-row `503`, exact empty revocation replay, and concurrency counterexamples; Clerk-human ordinary Agent Grant creation with visible-ASCII keys, normalized intent, pre-resource and post-authority replay, document/organization reauthorization, no-mandate grants, staged grant/event/empty recovery receipt, digest-bound secret-free reconstruction, corruption `503`, SQLite same-key evidence, and MCP/A2A issuance exclusion; MCP/A2A API-key management exclusion;
- single-active-stage Human Mode composition: `aria-current` stepper, Back/Next/Review/Release controls, one-shot heading scroll/focus with reduced-motion behavior, nonduplicating mobile dock, 44px controls, Work-mode fieldset/legend, BufferedCommitRegistry flush-before-transition, canonical Markdown-ref narrative serialization, native document-kind semantics, guided-card/frontmatter losslessness, timer/blur/cleanup flushes, malformed-Markdown fail-closed handling, no Monaco import, and owned-inventory state anchors;
- production Playwright Human Mode, public-content, crawler, anonymous-boundary, and egress gate: focused browser/egress contracts passed 18/18 and the full web Vitest suite passed 65 files/572 tests; TypeScript and ESLint passed. A physical non-reparse Temp copy using the existing `node_modules` (no install or external network) ran the official `npm run build:e2e`: Next 15.5.22 built, generated 21 pages, passed all 31 route-JS budgets, and emitted a validated receipt, `BUILD_ID`, and `standalone/server.js`. The existing Chromium production harness passed exactly 9/9 named tests only after sandbox escalation for browser spawn, covering public discovery, profile/resume/post HTML plus exact same-origin canonical Markdown byte parity with strong ETag/Content-Digest fixture responses, bounded positive anonymous Search, the public post author boundary, crawler/sitemap/robots contracts, default-off recruiting-route hiding, stage transitions, buffered narrative flush, canonical preview, `aria-current`/focus, 390px overflow, reduced-motion controls, serious/critical axe checks at desktop and the Resume/Post/Search mobile viewport, signed-out no-write boundaries, and anonymous/private route boundaries; the Next egress audit was empty. This is fixture/loopback/local-browser evidence only, not release readiness: live API, Clerk, PostgreSQL, Meilisearch, TLS, crawler indexing, manual assistive-technology, Docker image, clean revision, and fresh dedicated-VPS evidence remain unproven;
- built converter-image ingestion gate wiring: exact built-image identity and `network_mode: none`, valid PDF/DOCX/Markdown conversion, malformed and oversized rejection, protocol cleanup, and bounded timeout/crash supervision are required by the checker and CI workflow without being misreported as a local or deployment run;
- canonical public HTML source/version binding, distinct ProfilePage-to-Person versus explicit-name minimal resume DigitalDocument structured data, bounded metadata, safe resume omission without an explicit name, private-inference rejection, script-safe JSON-LD, partitioned sitemap limits/fallbacks, and robots parity;
- public `/trust` route ownership and semantic boundary: indexable plain-language current behavior, explicitly not a legal privacy policy and not an agent-authority grant;
- subject-scoped browser mutation fingerprints and ambiguous-only idempotency-key retention, including changed-subject, offline, and definitive-4xx counterexamples;
- private conversation per-subject/conversation coordinator scope reset and generation invalidation, synchronous cursor/send interaction claiming, owner-only release, retained-message refresh-failure compose/pagination gating until current-primary recovery, deferred race/scope/stale-release/duplicate/recovery tests, and non-persistence/logging anchors;
- subject-bound private network collection reads, authenticated-subject remounting, synchronous per-slice refresh epochs, current-ref precommit protection, stale-response rejection, independent initial-load guards, strict collection envelopes, refresh-versus-pagination exclusion, current-epoch cursor append, delivered-cursor deduplication, retained-data error truth, and current-success-only dependent-write anchors;
- durable human-only participant connection deletion: required idempotency key, replay-before-lookup ordering, transactional 204 empty receipt storage, lost-ack replay, and same-key collision counterexamples;
- durable private follow and content-block writes: shared visible-ASCII idempotency/OpenAPI, Clerk-human authority, pre-target and post-pair/target-lock replay, deterministic pair-row locking, current-target revalidation, quota/rollback ordering, two-direction follow removal on block, exact safe `200`/`204` response receipts, digest/state/target corruption `503`, collision `409`, SQLite same-key evidence, MCP/A2A exclusion, and synchronous subject-claimed Human Mode key/response/stale guards;
- shared late-existing idempotency receipt handling: object-identity-only provisional completion, loser rollback, committed-receipt replay, `ConcurrentIdempotencyReplay`, and no-winner-overwrite anchors bound to the concurrent reviewer decision test;
- fail-closed configured-search API readiness plus deployment/restore/health control ordering, including canonical public-site membership in Clerk authorized parties, source revision plus release receipt and API/web/Nginx image-identity preflight before the recorded API contract probe and writer stop, and their focused settings, environment, readiness, and operational-contract test anchors;
- frontend Docker-context policy: secret/generated-artifact exclusion with explicit `.env.example` and package/Dockerfile/app/component/library/public build-input retention, bound to `apps/web/.dockerignore` and operational-contract assertions;
- private-workspace shell runtime truthfulness: the request-rendered root layout evaluates the server-only complete-Clerk decision, the client header combines it with client auth configuration, partial configuration falls back to public navigation, and focused tests bind this behavior to the runtime-only `CLERK_SECRET_KEY` boundary without exposing the secret;
- public-profile secondary-read truthfulness: Agent Identity inventory failure preserves the public profile and canonical Markdown, discloses that identity status cannot be confirmed, exposes only a bounded directory retry, and never derives a private contact action from failed data; the feature registry and platform checker require the rendered regression test;
- semantic Compose Python-service hardening: duplicate-key rejection; exact image/build/entrypoint/command/healthcheck/mount/tmpfs/network rules; UID 10001, read-only root, cap-drop-all, no-new-privileges; explicit excluded-service exceptions; `cap_add`, privileged, writable-mount, and production-override counterexamples; and one CI verifier invocation after PyYAML is provisioned from `requirements-test.lock`;
- immutable numbered-release GitHub Action commits and multi-architecture OCI index/manifest-list digests across CI, Dockerfiles, and Compose, with checker counterexamples for mutable-ref regression;
- durable search-projection source contracts for version-keyed enqueue, current-only public indexing, tombstone/private removal, lease recovery, bounded retries/dead-letter health, restricted Compose credentials, fail-closed rebuild ordering, and index-name parity;
- public taxonomy route/model ownership, REST/MCP/A2A use of the shared taxonomy resolver and hit hydrator, Human Mode compact-alias-only serialization and fail-closed parsing/state anchors, aggregate selection/facet budgeting, representative evidence caveats, scalar singleton GET `location_id` duplicate rejection, strict optional exact-search/taxonomy cursor omission/null starts plus duplicate/blank/whitespace/oversized/malformed rejection and signed continuation bounds across REST/OpenAPI/MCP/A2A, focused catalog/POST/protocol/web test anchors, and references to existing operational-contract ordering;
- public chronological post inventory and sitemap category 3 metadata-only behavior, text/markdown OpenAPI responses for public post reads, plus the six private moderation-review HTTP routes (case/appeal queue, detail, and decision) bound to reviewer authority, `include_in_schema=False`, no-store headers, strict cursors, and idempotent decision receipts; these remain repository-only and are excluded from public, MCP, A2A, and Agent Card discovery;
- hidden verified recruiting evidence review: configured non-impersonated Clerk reviewer authority, private no-store/noindex headers, bounded digest-verified artifact bytes, discovery exclusion, and fail-closed artifact staging/commit/reconciliation/readiness anchors across the 0006/0007 migrations and focused API/web tests; this is repository evidence only and does not prove live artifact durability or reviewer deployment;
- migration `0028_scrub_verification_change_payloads.py` is bound to the verified-recruitment privacy contract: only canonical `organization_verification.submitted` events are rewritten in ordered bounded batches, state-only output is enforced, malformed/duplicate-key payloads fail closed, and the irreversible downgrade boundary plus both migration tests are checked. This remains repository/SQLite evidence, not live PostgreSQL migration or historical-event proof;
- the named contact-request status constraint, fail-closed pre-DDL migration preflight, fixed non-data error, rollback/version preservation, downgrade/re-upgrade behavior, and exact persisted versus projected status boundary;
- canonical same-origin public-profile auth returns, Clerk sign-in/sign-up handoff props, explicit post-auth mutation confirmation, and encoded/dot-segment/open-redirect counterexamples;
- protected A2A contact/outreach/status error-state mappings, canonical lowercase UUID parity for MCP/A2A outreach status against the HTTP authority, exact privacy-minimal artifact shape, required OpenAPI idempotency headers, Agent Card credential/required-header authority, valid-envelope invalid-action terminal rejection, 403/404 non-enumeration, precondition handling, and the boundary between action artifacts and outer protocol errors;
- Agent Identity create/withdraw receipt durability: Clerk-human-only visible-ASCII caller keys, atomic safe event plus existing IdempotencyRecord receipt, digest-bound exact `201`/empty `204` replay, collision/corruption/state-drift failure, public removal after withdrawal, and API-key/Agent-Grant/MCP/A2A exclusion;
- MCP `send_agent_outreach` and `get_agent_outreach_status` parity with canonical HTTP: exact-origin mandate/source-handle/grant digest binding, live reauthorization, strict schemas/dispatch, caller-key and privacy-safe envelopes, capabilities/`/llms-full.txt` discovery, human-only decisions/no external delivery, and the unchanged seven-skill A2A Agent Card boundary;
- private outreach policy/inbox initial guards, current-subject refresh epochs, synchronous refresh-versus-pagination exclusion, stale-response suppression, retained-data error truth, and cursor deduplication;
- agent-aware search contracts for canonical authorization before set-based SQL enrichment, bounded handle/capability references, protocol parity, and exclusion of AgentIdentity and its selector from Meilisearch;
- single, global, and profile-inventory Agent Identity HTTP/MCP/A2A read parity through live public identity/directory helpers, eligibility-reauthorized cursor anchors, bounded schemas/cursors, duplicate/blank/whitespace/oversized/malformed rejection, signed continuation bounds, privacy allowlists, discovery-only caveats, Agent Card/capabilities/LLM anchors, and HTTPS skill-set smoke parity;
- Clerk-human-only employer inventory contracts for membership-scoped SQL, signed subject/endpoint cursors, privacy-minimal summaries, exact-resource reloads, and protocol/crawler exclusion;
- private organization membership invitation, acceptance, and removal: visible-ASCII idempotency/OpenAPI, Clerk-only authority, owner-plus-organization locking before invitation replay, current-owner authority before removal replay, organization-to-membership lock order, original-invite request binding, generation-bound invite/accept receipts, exact empty removal replay, corruption/substitution/reincarnation/former-owner/concurrency counterexamples, and MCP/A2A exclusion;
- durable application withdrawal and employer review/accept/reject: visible-ASCII idempotency/OpenAPI, Clerk-human authority, organization → job → application lock order, current recruiting authority before employer replay, applicant owner-bound pre/post-lock withdrawal replay, hashed applicant/decision/snapshot plus exact-response digest reconstruction, empty receipts and status-only events, collision/competing transition coverage, corruption/deletion/relationship/status/retention/snapshot `503` failure, independent submission-receipt preservation, SQLite/source-only evidence, and MCP/A2A exclusion;
- durable private contact policy and decision writes: `PUT /v1/contact-policy` and `POST /v1/contact-requests/{contact_request_id}/{action}` ownership, visible-ASCII keys, body-plus-mandatory-exact-strong-`If-Match` fingerprints, mandatory `If-Match` and `200` `ETag` OpenAPI, replay before stale-precondition evaluation, owner-namespaced PostgreSQL advisory serialization, web ETag/If-Match/precondition-bound logical attempts, authority before pre-lock replay, non-Clerk `agent_outreach` exclusion before the request lock, pre/post-lock replay, `ContactRequest` → `ContactPolicy` → `ContactBlock` lock order, safe policy and empty hash-bound decision receipts, report-reason privacy, corruption/deletion/retention failure, real opaque-404 nonhuman coverage, four focused repository tests, and MCP/A2A decision exclusion;
- lifecycle confirmation/terminal receipt proof: route-private non-impersonated Clerk claims rather than general Principal access; versioned deletion/subject/key confirmation HMAC without raw inputs; strict exact `202` replay; no-store/noindex receipt status; terminal deny, tombstone, journal/witness/live-mirror, provider, completed-item, and backup-obligation proof before receipt-rate mutation; migration cleanup preserves lifecycle/deny authorities while terminal markers expire; plus default-disabled parity across API settings, `.env.example`, Compose, frontend build/runtime configuration, and disabled-path tests.
- exact strong document If-Match parity: wildcard, weak, and list validators fail closed across HTTP, MCP, and the document service, while replay remains the first state-dependent consultation; this is repository evidence, not a live PostgreSQL race or deployed client proof.
- unique, feature-specific repository receipt `check_id` coverage for the controls above.

## Still manual or unverified

| Coverage | Why the registry is insufficient | Closure evidence |
| --- | --- | --- |
| Authority semantics | An actor list cannot prove every handler enforces ownership, consent, scope, denial, and audit rules. | Focused positive and negative authorization tests plus reviewer traceability. |
| Direct feature behavior | A test path proves that a file exists, not that every declared route or failure mode is exercised. | Route-to-test coverage and acceptance evidence for the feature's exact current surface. |
| UI truthfulness and accessibility | The final local production-build/Chromium receipt exercises rendered Human Mode controls, buffered transition timing, keyboard focus, reduced-motion/mobile behavior, signed-out/private and no-write boundaries, public profile/resume/post/Search pages, and serious/critical axe checks at desktop plus the Resume/Post/Search mobile viewport. It does not replace manual assistive-technology review or live identity/API evidence. | Manual screen-reader/assistive-technology review, live Clerk/API checks, and a fresh dedicated-VPS browser receipt. |
| Private shell build/runtime parity | Source and rendered-component checks prove that the root layout is request-rendered and that partial configuration hides private links. A local production artifact built with a publishable key and no secret was served twice: the runtime-secret case rendered `/network` and `/agents`, while the no-secret case rendered `/agent-directory` and omitted both private links. This proves local build/runtime environment re-evaluation, not the Docker image, a live Clerk session, mobile authenticated navigation, or destination authorization. | Build the Docker image with the exact Compose arguments, run with the runtime-only secret and a live Clerk session, verify desktop/mobile private navigation and destination authorization, then repeat with each key missing and confirm public fallback plus bounded private-route denial. |
| Cinematic Human Mode progression | The Human Mode portion of the local exact-9/9 Chromium production-harness run covers every progressive stage, Back/Next/Review/Release flow, pending narrative flush, canonical preview, one-shot focus, `aria-current`, 390px overflow, and the mobile reduced-motion path. The official local `npm run build:e2e` in a physical non-reparse Temp copy using the existing `node_modules` emitted a validated receipt, `BUILD_ID`, and `standalone/server.js`; the harness required sandbox escalation only for browser spawn. This fixture/loopback evidence does not measure screen-reader announcements, touch-target geometry, performance, live Clerk/API behavior, Docker image behavior, or deployment. | Manual assistive-technology and performance review plus live identity/API and fresh dedicated-VPS evidence. |
| Public mirror and crawler behavior | The final local 9/9 production-build/Chromium gate renders public profile, resume, and post HTML, verifies exact same-origin canonical Markdown links and byte parity with strong ETag/Content-Digest fixture responses, checks the public post author boundary and bounded anonymous Search, and covers robots plus all current sitemap categories. It does not prove live API parity, sanitizer/cache behavior against the deployed service, crawler consumption, or search-engine indexing. | Browser and HTTP rendering checks against a live API plus crawler validation of deployed sitemap consumption and indexing. |
| Browser retry under real identity rotation | The helper and unit tests prove local subject-scoped retry semantics, not timing across live Clerk token/session rotation or ambiguous network delivery. | End-to-end mutation tests that rotate the authenticated subject and inject acknowledgement loss while verifying server idempotency records. |
| Live Clerk authorized-party enforcement | Production API settings and the CI-wired shell contract require the canonical public site origin in the authorized-party list while permitting additional origins. This is local configuration evidence, not proof that Clerk emits the expected `azp` or that deployed authenticated requests succeed. | On the fresh dedicated VPS, authenticate through the canonical browser origin, verify the issued token's authorized party, and exercise representative private API reads and writes over HTTPS. |
| Durable idempotency under live database interleavings | The repository checker and focused tests bind connection-delete replay and late-existing receipt rollback, but do not prove PostgreSQL lock scheduling, process failure after commit, or a live lost-ack retry. | Targeted PostgreSQL concurrency and acknowledgement-loss runs that verify one committed receipt, one domain event, no loser mutation, and exact replay bytes under the deployed transaction settings. |
| Follow and content-block durability | Focused repository tests use SQLite; same-key `gather` does not prove PostgreSQL pair-row lock scheduling, process loss after commit, live Clerk subject rotation, browser mutation timing, or third-party MCP/A2A client exclusion in deployment. | Targeted PostgreSQL contention across follow/unfollow/block/unblock, interrupted post-commit acknowledgement, target-state substitution attempts, Clerk rotation, browser retry, and deployed MCP/A2A negative probes that verify one safe receipt/effect set and exact replay bytes. |
| Agent Grant issuance durability | The focused creation/recovery suite runs against the repository SQLite fixture. Its same-key `gather` assertion does not prove PostgreSQL row-lock scheduling, acknowledgement-loss recovery, or a crash between receipt commit and response; the three retained protocol callers are compatibility coverage, not proof of the durability contract. | PostgreSQL integration runs with contending same-key creates, interrupted post-commit acknowledgement, document/organization authority loss, grant/receipt corruption, and verification of one grant, one safe event, one empty receipt, and exact safe replay under deployed transaction settings. |
| Discovery agreement | The checker now compares repository-literal capabilities MCP/A2A inventories, Agent Card action examples, `/llms-full.txt` operation mentions, and the hidden/advertised OpenAPI state of discovery routes. It does not execute or compare live generated responses. | A fresh dedicated-VPS HTTPS/OpenAPI, MCP `tools/list` and client-call, A2A Agent Card/action, and `/llms-full.txt` retrieval run against one deployed revision, with intentional transport differences reviewed separately. |
| Public taxonomy protocol and Human Mode parity | The checker validates ownership plus source and test anchors; the Human Mode portion of the local exact-9/9 Chromium production-harness gate covers the rendered journey, while this row does not prove equivalent HTTP/MCP/A2A results, registry readiness behavior, current public term contents, fresh-VPS/Docker/Clerk/live-API behavior, or deployment. | Focused API parity tests plus deployed HTTPS HTTP/MCP/A2A calls against known public terms and a deliberate non-ready condition; manual assistive-technology review remains separate. |
| Live MCP interoperability | Strict tool schemas and focused dispatcher tests do not prove a third-party MCP client can initialize, discover, authenticate, retry, and recover writes over deployed HTTPS. | Recorded client-level MCP create/update/proposal runs against the fresh dedicated VPS, including replay and proposal-only denial evidence. |
| Agent Identity and mandate-bound MCP outreach durability | The identity and MCP checks are repository/SQLite evidence. They do not prove PostgreSQL transaction scheduling, post-commit acknowledgement loss, live Clerk/Agent Grant rotation, third-party MCP-client behavior, or public-cache withdrawal convergence. | Fresh dedicated-VPS PostgreSQL and HTTPS MCP runs that contend same-key identity writes/outreach, interrupt acknowledgement after commit, revoke a mandate/identity, and verify exact safe replay, public removal, and privacy-minimal responses. |
| Migrations and compatibility | Source ordering proves the API and lifecycle worker are stopped before the migration command, but not that a real database migration, failed rollout, rollback, or mixed-version transition succeeds. | Fresh and upgrade migration tests, rollback plan, and compatibility evidence. |
| Built converter-image execution | The checker now binds the isolated built-image conversion script and its exact CI invocation, but this Windows checkout has no Docker daemon and no clean revision whose CI result can serve as an execution receipt. | A passing Linux CI run for the exact clean revision, followed by the same valid/malformed/oversized/timeout/crash workflow and network-isolation inspection on the fresh dedicated VPS. |
| API OS-package reproducibility | The API Dockerfile now uses the immutable `20260805T010740Z` Debian trixie snapshot and exact direct versions for `libmagic1t64`, `poppler-utils`, and `tesseract-ocr`, with static fail-closed tests and installed-version checks. This Windows checkout cannot build or inspect the image, and the evidence does not inventory transitive operating-system packages. | Build the exact clean revision on Linux, verify installed direct versions on every supported target architecture, generate and review a complete image SBOM, then run the converter built-image gate on the fresh dedicated VPS. |
| Worker operation | Source and focused tests cover the search worker contract, but do not prove live PostgreSQL grants, scoped Meilisearch permissions, scheduling, container isolation, convergence, or operational recovery. | Fresh-target runtime drills and recorded credential-denial, health, retry, failure, non-public/tombstone removal, and recovery evidence. |
| Agent-capability search completeness | The SQL enrichment is authoritative for eligible references within the candidate set, but the underlying search deliberately stops at 1,050 Meilisearch candidates. | Narrow taxonomy queries plus deployed HTTP/MCP/A2A checks that preserve the candidate-window warning; never treat a returned reference as outreach authority. |
| Live employer tenant isolation | Source and focused tests prove the Clerk-only predicate, signed cursor binding, summary allowlist, and subject-keyed UI state, but not live Clerk token rotation or PostgreSQL multi-tenant behavior. | Browser/API runs with two real Clerk subjects, owner/admin/member/revoked roles, cursor misuse, and proof that no application or snapshot read occurs during inventory loading. |
| Organization membership durability | Invitation, acceptance, removal, corruption, and same-key concurrency tests run against the repository SQLite fixture; SQLite does not prove production PostgreSQL row-lock scheduling, acknowledgement-loss recovery, or concurrent owner-transfer/removal behavior. | PostgreSQL integration runs with contending invite/accept/remove and owner-transfer transactions, process interruption after commit, and exact receipt/event/replay verification under deployed isolation settings. |
| Application transition durability | The CI-wired live-stack test now forces one applicant withdrawal and employer acceptance to wait behind the same PostgreSQL organization-row gate, then verifies one terminal response, one transition receipt, two owner-scoped events, the acceptance-only notification, and the employer-detail withdrawal boundary. This Windows checkout cannot execute that opt-in PostgreSQL test, and the repository still has no current clean-revision receipt for it. Review/reject contention, interrupted post-commit acknowledgement, live Clerk rotation, and deployed transaction behavior remain unproven. | A passing exact-revision Linux CI receipt for the forced withdrawal/acceptance interleaving, plus PostgreSQL runs covering review/reject, interrupted acknowledgement, authority loss, snapshot corruption, relationship substitution, and exact replay under deployed isolation settings. |
| Contact policy and decision durability | The four focused contact-durability tests use the repository SQLite fixture. Same-key `gather` is not forced SQLite or PostgreSQL lock scheduling, and the different-key decision transition is sequential, so it does not prove PostgreSQL serialization. No live Clerk, acknowledgement-loss, report-reason retention, MCP/A2A client, or dedicated-VPS exercise has run. | PostgreSQL integration runs that contend same- and different-key policy/decision writes, interrupt after commit before acknowledgement, test agent-outreach opaque denial and retention expiry, and verify one durable receipt/event set plus exact replay under deployed isolation settings. |
| Deployment and recovery | Restore preflight/order anchors are not evidence of live Docker behavior, a restored service, TLS, isolation, or rollback. | Target-scoped deployment, restore, smoke, and rollback records tied to an exact revision and configuration on a newly provisioned dedicated VPS. |
| Lifecycle confirmation and terminal proof | Route/private-claim, HMAC, migration, model, journal/mirror, provider, backup-obligation, and focused-test anchors do not prove real Clerk step-up, provider deletion, witness durability, worker execution, PostgreSQL lock scheduling, terminal cleanup, or a post-concealment lost acknowledgement. | Targeted PostgreSQL and fresh dedicated-VPS runs with real Clerk/provider fixtures, interrupted acknowledgement, journal/witness and mirror validation, backup-expiry proof, terminal status, and cleanup evidence. |
| Frontend Docker context | `.dockerignore` and operational-contract source checks do not prove the actual Docker build context, image layer contents, build cache behavior, or secret absence in a created image. | A local controlled Docker context/build inspection and an independently reviewed fresh dedicated-VPS image receipt, without exposing secrets. |
| Compose container hardening | The YAML semantic verifier and CI wiring do not start a Compose project or inspect created containers. They cannot prove kernel capabilities, mount flags, UID mapping, read-only roots, tmpfs size, health execution, or service isolation at runtime. | A controlled local Compose configuration/build/container inspection followed by a fresh dedicated-VPS runtime receipt for all eight protected Python services. |

- impersonated Clerk authority: `optional_principal` rejects mutation requests after Clerk verification but before account/resource lookup, and `test_impersonation_authority.py` proves no credential, organization, document, identity, idempotency, or change-event persistence while ordinary Clerk access remains valid. This is repository-only evidence; live Clerk session impersonation and deployed boundary behavior remain unproven.

The account-lifecycle feature remains `feature_gated`. Its active API receipt/status work stays hidden by the literal default-false gate; confirmation and terminal-proof anchors are repository evidence only. Empty deployment evidence and an `implemented` or `feature_gated` stage never authorize a deployment, erasure-completion, or release claim.

Without a clean committed `HEAD`, no repository receipt can bind to a current revision. This scaffold records no live-Docker or fresh-VPS evidence, so those advanced claims remain unavailable.
